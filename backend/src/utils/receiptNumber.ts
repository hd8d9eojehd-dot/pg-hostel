// Re-export from studentId.ts for backward compatibility
export { generateReceiptNumber, generateInvoiceNumber, generateStudentId, generateComplaintNumber, generateOutpassNumber } from './studentId'
